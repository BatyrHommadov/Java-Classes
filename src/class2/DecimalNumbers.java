package class2;

public class DecimalNumbers {
    public static void main(String[] args) {
        /*
        if we want to store decimal numbers in computer's memory using Java, we can use following types of boxes:
        float
        decimal
         */
        float myNumberBox=10.5f;
        double largeDecimalBox=134545455.12121;
        System.out.println(myNumberBox);

        /*
        if we want to store characters (#, £, o s, 4) we can use char datatype
         */
        char letter='l'; //char can hold only one character
        char gender='M';
        char symbol='#';
        char numberChar='1';

        /*
        String datatype belongs to a different category called non-primitives, that's why for String rules are a bit
        different. For example: S of String is uppercase, its colour is also not blue, this will be discussed later.
         */
        String name="Sam";

        boolean hungry=true;
        boolean sleepy=false;

    }
}
